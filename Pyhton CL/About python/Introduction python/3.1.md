# Making decisions in Python
## Comparison: equality operators "=="
- = is an assignment operator, e.g., a = b assigns a with the value of b;
- == is the question are these values equal? so a == b compares a and b.
- If we want to say “not equal,” we use the != symbol.
## Operators that compare greater and smaller values.
- To compare whether a value is bigger or smaller, we use the < and > symbols.
- If we want to compare whether a value is equal to or bigger/smaller than another value, we use the <= and >= operators.
## Operators 
- ** - Exponentiation operator.
- // - Floor division operator.
- % - Modulo operator.
## Conditions and conditional execution
When we want a program to execute or not execute something based on a condition, we use a conditional operator.

This structure:

if true_or_not:

(Tab)do_this_if_true


## If -  else operator 
When we define a condition, we use the else statement together with if to execute the alternative action when the condition is false.


if true_or_false_condition:

(Tab)perform_if_condition_true

else:
  
(Tab)erform_if_condition_false

##  The elif statement 
When we want to set not just one condition but several conditions, we use the if statement for the first condition and the elif statement for the following conditions.
  - Structure if elif

if true:

  print()

elif false:

  print()


  
