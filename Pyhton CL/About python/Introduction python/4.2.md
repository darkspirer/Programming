# How function communicate with their environment 
## Parameterized functions
In def function(name):, name is a parameter. When we call the function, we pass an argument to that parameter.

A function can have two parameters. This means we can pass two arguments when calling the function. In def function(first_name, last_name):, the two parameters are separated by a comma.
