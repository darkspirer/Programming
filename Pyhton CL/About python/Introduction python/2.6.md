# The input() function
The input() function is able to read data entered by the user and to return the same data to the running program.

We can use the input() function to enter data in the output section.

The required data can be entered using input(), and we can also present the input as a question in the output.

But there are also cases where it gives an error.

For example.

In the output section, we cannot directly enter an integer (int) value.

In this case, we need to add the int conversion before the input() function, like this: int(input()).

## Information fype
Python offers two simple functions to specify a type of data and solve this problem ‒ here they are: int() and float().

Their names are self-commenting:

the int() function takes one argument (e.g., a string: int(string)) and tries to convert it into an integer; if it fails, the whole program will fail too (there is a workaround for this situation, but we'll show you this a little later);
the float() function takes one argument (e.g., a string: float(string)) and tries to convert it into a float (the rest is the same).

## String 
- We can combine several strings or sentences of string type by using string + string.
- Or, by using mathematical operations with numbers, we can also repeat a string or sentence multiple times.
