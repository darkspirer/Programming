# C++ first code 
In C++, it always starts in this form

#include < iostream>

using namespace std;

int main(){
}

- {} - The code part is written inside these brackets/braces

# The basics of Integers and variables 
- integers, that is, those which are devoid of the fractional part;
- floating-point numbers (or simply floats), that contain (or are able to contain) the fractional part.

## Variables
What does every variable have?
- a name;
- a type;
- a value;

##### Rules we need to follow when declaring a variable

- the name of the variable must be composed of upper-case or lower-case Latin letters, digits and the character _ (underscore);
- the name of the variable must begin with a letter;
- the underline character is a letter (strange but true);
- upper- and lower-case letters are treated as different (a little differently than in the real world – Alice and ALICE are the same given names but - they are two different variable names, consequently, two different variables);

When we declare a whole number as a variable, we define it using int:

- int variable_name

### And the thing we need to pay attention to most is that we almost always need to put a ; (semicolon) at the end of each line of code.

### We can consider keywords as the reserved words/operators used in code — these cannot be used as variable names

## Comments
The C++ language supports two ways of inserting comments:

- // comment - line comments

- /* comment */ - block comments.
