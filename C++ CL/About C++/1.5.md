# Making Simple Decisions: Intro to Flow Control in C++



Computers always answer us with a yes or a no; they never give an 'I don't know' answer
-
### Operators 
- The == operator represents equality
- != is used to express 'not equal'
- The > symbol is used for 'greater than', and if it is 'greater than or equal to', we use >=
- The < symbol is used for 'less than', and if it is 'less than or equal to', we use <=


### Conditions and conditionals executions
In C++, we use `if` in conditional statements.

##### if(true_or_not) do_this_if_true;

This is how it is used.
Inside the parentheses, we put a condition that evaluates to either true or false,
and outside the parentheses, we write the command or task that should be executed.

- And when writing the task we want to be executed, we must always use curly braces {}.
